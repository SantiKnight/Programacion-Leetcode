import math
class Solution:
    def maxDistance(self, position: list[int], m: int) -> int:
        # suma = 0
        # position = sorted(position)
        # for i in range(len(position)):
        #     print(position[i])
# s = Solution()
# s.maxDistance([1,7,3,4,2],3)